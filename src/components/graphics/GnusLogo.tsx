// ---- GNUS Logo (inline SVG — stable) ----
export function GnusLogo({ size = 32 }) {
  return (
    <img
      src="gnus-logo-64.png"
      alt="GNUS logo"
      width={size}
      height={size}
    />
  );
}
